# 2026-04-16

## Current task
Iter5 complete. Planning iter6.

## Progress
- **Iter1 (ResMLP)**: val/l2=1.27 — plateaued. DISCARDED.
- **Iter2 (Transolver d=256, M=64, 6 blocks)**: val/l2=**1.0751**. KEPT.
- **Iter3 (Transolver d=384, M=128, 8 blocks)**: val/l2=1.086 at epoch 60/90. DISCARDED — undertrained.
- **Iter4 (per-step decoder + v-tendency)**: val/l2=1.2040. DISCARDED — new decoder had 1/5 capacity of iter2's.
- **Iter5 (distance-to-airfoil feature, cached per geometry)**: val/l2=**1.0554** at epoch 110/120. KEPT. Beats iter2 by 0.0197.

## Decisions
- Iter5 adds unsigned min-distance to airfoil as 14 point features (raw, log1p, 6 Fourier pairs), cached via pos-fingerprint (162 unique geometries across 810 samples).
- Kept iter2 backbone (3.28M params) — capacity spent on physical prior instead of raw size.

## Blockers
- git push fails (no creds). Predictions saved to PVC → scorer picks them up.

## Next
- Commit iter5 best.pt + journal.
- Iter6 candidates (ranked):
  1. **k-NN local attention block** after Transolver — captures fine turbulence via local attention on nearest-neighbor point pairs (e.g. k=16). Adds ~200k params, should fit budget.
  2. **Test-time x-flip augmentation** — wings are ~symmetric about centerline; average predictions from original + x-flipped input. Zero extra training cost; can stack on iter5 checkpoint.
  3. **Pressure-field auxiliary loss** — if dataset has pressure available, multi-task learning may regularize velocity. Check data.py.
  4. **Dropout inside Transolver blocks** — iter5 shows mild overfit (train=1.50 vs val=1.056). Try p=0.1 on attention output.
- Lean toward (2) first — nearly free, then (1) for architectural push.
